# Developer Tools

Tools are repository executables, not runtime dependencies or separately versioned packages.

Planned tools include an ETF inspector, distribution-frame inspector, and EPMD diagnostic client. Each tool must consume the same public battery modules used by applications; duplicate protocol parsers are prohibited.

No tool is implemented yet. Add a build target only with its first runnable command and smoke test.
